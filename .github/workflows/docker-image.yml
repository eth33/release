name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag ql-made-easy:$(date +%s)
    - name: Docker Login
      run: docker login -u y3qx -p dckr_pat_4tVwcm3v6-i0yk3EqikyDsSrKyk
    - name: Publish Image
      run: docker push docker push y3qx/codeql-made-easy:$(date +%s)
