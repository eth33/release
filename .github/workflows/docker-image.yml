name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --tag y3qx/ql-made-easy:latest
    - name: Login docker
      run: docker login -u y3qx -p dckr_pat_Wbsuyu1CIC6g501ySv75p_fhxuY
    - name: Push image
      run: docker push y3qx/ql-made-easy:latest
